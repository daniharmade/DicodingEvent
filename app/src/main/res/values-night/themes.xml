<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.DicodingEvent" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimaryDark</item>
        <item name="colorPrimaryVariant">@color/colorPrimary</item>
        <item name="colorOnPrimary">@android:color/black</item>
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorOnSecondary">@android:color/black</item>
        <item name="android:windowBackground">@color/black</item> <!-- Background for dark mode -->
        <item name="android:textColor">@android:color/white</item> <!-- Default text color for dark mode -->
    </style>
</resources>
