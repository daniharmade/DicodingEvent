<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.DicodingEvent" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorOnSecondary">@android:color/white</item>
        <item name="android:windowBackground">@color/white</item> <!-- Background for light mode -->
        <item name="android:textColor">@android:color/black</item> <!-- Default text color for light mode -->
    </style>
</resources>
